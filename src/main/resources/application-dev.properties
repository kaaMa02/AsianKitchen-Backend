# SERVER
server.port=8080

# DATASOURCE (local)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/asiankitchen?sslmode=disable
spring.datasource.username=localuser
spring.datasource.password=localpass

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# FLYWAY
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# uncomment if this is a BRAND NEW empty DB with no flyway history:
# spring.flyway.baseline-on-migrate=true

# JWT (dev defaults)
jwt.secret=rLaomV79z8iQ/uNrUFKTsw8ooTaDtymWZe77Z5wQhiE=
jwt.expiration-ms=3600000

# MAIL (disable for dev if you want)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
app.mail.from=no-reply@local.test
app.mail.to.contact=owner@local.test
app.mail.to.bookings=owner@local.test

# STRIPE (dev ? not used locally unless you test)
stripe.secret-key=sk_test_xxx
stripe.webhook-secret=whsec_xxx
stripe.currency=chf

# --- Security / Cookies ---
app.security.auth-cookie-name=AK_AUTH
app.security.cookie-domain=localhost
app.security.cookie-secure=false
app.security.same-site=None

# CORS origins (you already had this ? ensure FE dev URL is here)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173